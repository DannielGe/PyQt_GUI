KNN定义：
KNN是一种常见的监督学习算法，给定测试样本，基于某种距离度量找出训练集中与其最靠近的k个训练样本，然后基于这k个“邻居”的信息来进行预测。
步骤：
1.计算距离：给定测试对象，计算它与训练集中每个对象的距离，空间距离的计算方法有多种，有欧式距离、夹角余弦（多在文本分类中使用）等。
2.找邻居：圈定距离最近的k个对象，作为测试对象的近邻。
3.做分类：根据这k个近邻归属的主要类别，对测试对象进行分类。